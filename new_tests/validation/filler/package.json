{
  "name": "gopdfsuit-validation-filler",
  "private": true,
  "type": "module",
  "description": "Playwright E2E: flujo comprobar → rellenar → portada → fusionar → validar",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
